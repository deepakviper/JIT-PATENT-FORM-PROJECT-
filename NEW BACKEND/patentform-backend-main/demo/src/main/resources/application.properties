spring.application.name=demo

# Set the server port
server.port=${PORT:8080}

# Increase maximum file upload sizes for large patent drafts
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
